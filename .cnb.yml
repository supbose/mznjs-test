# .cnb.yml
$:
  vscode:
    - runner:
        cpus: 4       
      docker:
        build: .ide/Dockerfile
        image: cnbcool/default-dev-env:latest
      services:
        - vscode
        - docker
      stages:
        - name: ls
          script: ls -al
